---
name: python-code-quality
description: Python コードを書く・レビューする・リファクタリングするときは必ず参照する規約。「コードを綺麗にして」「レビューして」という依頼でも使う。
---

# Python コード品質規約

- 意味を持つ数値や文字列は、モジュール冒頭で大文字スネークケースの定数にする。
- docstring とコメントには処理の言い換えを書かず、他の書き方を選ばなかった理由（Why not）を書く。
- 書き終えたら、環境にあれば `ruff check` と `ruff format` を実行する。
